# Pitchkit

**Docs:** [README](./README.md) · [plan](./PLAN.md) · [architecture](./ARCHITECTURE.md) · [data](./DATA.md) · [glossary](./GLOSSARY.md) · [AGENTS](./AGENTS.md)

Creator media kits at **pitchkit.app**. Humans start at [README.md](./README.md). Spec: [PLAN.md](./PLAN.md). Picture: [ARCHITECTURE.md](./ARCHITECTURE.md). Columns: [DATA.md](./DATA.md). Stats vocabulary: [GLOSSARY.md](./GLOSSARY.md).

## Product lock

- Kit URL: `pitchkit.app/k/[handle]`. Public on first successful connect. Handle frozen by default; IG rename does not move our URL unless they opt in on reconnect (WHA-313: **Update kit URL to @{new}**; default = keep; old `/k/…` 404s; collision `-2`; keep `.` `_`). Missing/disconnected → 404.
- Instagram is login (Professional only — Business **or** Creator; Personal cannot power Insights; `/?error=personal` + PROFESSIONAL copy). Pitchkit session = httpOnly cookie (`pitchkit_session`). Continue GET/POST `/auth/instagram` is Instagram Business Login when `IG_APP_ID` + `IG_APP_SECRET` exist; otherwise stub seed `demo` (not an Instagram token; seed tokens stay null). When live secrets exist, `resolveSession` returns null for leftover seed `demo` so `/` shows Continue with Instagram and `/insights` goes home (public `/k/demo` stays). Failed OAuth finish clears `pitchkit_session`. `writeGraphSnapshot` tries SQL then KV `graph:` (never refuse KV solely because Hyperdrive is bound; thrown SQL → false → KV); false → no session cookie, `/?error=persist`. After a true write, set `pitchkit_session` to the in-memory handle on a single `Set-Cookie` — do not re-read via `resolveSession` (a second Set-Cookie on that 303 can fold on OpenNext/Workers and leave leftover `demo`). Landing OAuth errors: `oauth_state` / `oauth_exchange` / `oauth_me` / `personal` / `oauth_poll` / `persist`. If Insights poll fails after `/me`, persist a minimal snapshot (empty media/reach/audience) and still log in. If `resolveSession` succeeds, `GET /` redirects to `/insights`. `/insights` without the cookie → `/`. Sign out `/auth/sign-out` clears the cookie (kit stays public). Disconnect `POST /auth/disconnect` clears the cookie, stamps `disconnected_at` on the Graph snapshot, nulls tokens, and 404s the kit. Reconnect re-runs the same path (WHA-313 `update_handle=1` optional; default keep; no rename UI on the stub). Live finish never sets `pitchkit_session=demo` when `/me` username is not `demo` — a Graph snapshot stuck on handle `demo` escapes via unique-handle rules and is rewritten under that handle. `/k/[handle]` is public (no cookie).
- Connect **before** the button (`disclosure_version` = 1):

  > We only use your public posts and Instagram Insights to build your media kit. We don’t read DMs, who you follow, or unfollowers. Disconnect deletes your kit and the copies we stored.

  Landing CTA is disclosure + Professional note + **Continue with Instagram** for signed-out visitors. A resolvable `pitchkit_session` redirects to `/insights` — do not invent a second landing chrome. One quiet demo-session line under the button is OK (`DEMO_SESSION_NOTE`) when secrets are missing. Do not lead with stub-token / “no Instagram token” language. Live secrets start Instagram OAuth and must not treat leftover `pitchkit_session=demo` as signed-in; missing secrets still set the seed `demo` session.

- After login: **Insights**. Chrome copies WMDS Pattern — creator Insights Show code (`examples-pitchkit--creator-insights`): `<main className="grid-page min-h-screen bg-body [--grid-column-gap:8px] [--grid-max:1140px] [padding-bottom:44px]">`, header `band pb-4` with three-column `grid-cols-[1fr_auto_1fr]` (PitchKit label + default hug `SegmentedControl` + `Avatar` in `justify-self-end`), body `band pt-6 sm:pt-8` / inner `band min-w-0 gap-y-6 sm:gap-y-8`. PitchKit segment is Pattern — owner PitchKit Show code (`examples-pitchkit--owner-pitch-kit`: 1:1 shareable sections + CreatorIdentityStrip + hide/restore posts + intro/past-brands editors; Coming soon retired) so the selected-pill can move — do not hard-nav `/insights` ↔ `/k/…` and do not mount `KitEdit` there. Full-bleed `<body className="bg-body min-h-screen">`. No `layout="stretch"`, no `w-full` on the control, no AppFrame `min-h-dvh py-6` surface on Insights, the public kit, or Settings. `GridOverlay` and `ExampleGridControls` stay Storybook-only. One `<Toaster position="bottom-right" />` at `app/layout.tsx`. Insights body matches the Pattern: `PageHeader`, four-up `Stat` (`col-span-2 md:col-span-4 lg:col-span-3`), outlined `Chart.Cartesian` + `Chart.RankedBars`, **Recent proof** via `Tab.Group`, `MoreMenu` + `AlertDialog` hide + `toast.add()` Undo through `hideFromKit(mediaId)` / `restoreToKit(mediaId)` (`POST /api/media/hide` + `POST /api/media/restore`). Product toasts pass title + description (Share kit, hide, restore, hide/restore errors). Spell **Engagement rate** (never “ER”). Formula lives on the Engagement rate Stat info `Tooltip` (`ENGAGEMENT_FORMULA`), not a PageHeader headline. Share kit stays on Insights PageHeader (Pattern still shows it) and on the owner PitchKit PageHeader. No invented trends. Insufficient reach series keeps the Reach Card empty band (muted **Badge** “No data” → title → body, title↔subtext `gap-2`); Graph-unavailable omits the optional chart. Empty audience mixes keep the Audience Card insufficient well (muted **Badge** “No data” → `No audience data yet` / `Connect Instagram Insights demographics when available.`, same `gap-2`) — never EXAMPLE % or `audience ?? SEED_AUDIENCE`. Both empties can show at once. First-connect loading is the skeleton Pattern; Refresh retrieving is Header + `Chart.Loading`. No AppShell. Avatar opens Account settings Dialog (`examples-pitchkit--account-settings-owner`); Delete lives there, not the footer. Brands never see Insights. Public `/k/demo` omits `reach_series`. Owner demo includes seed/example Insights + `reach_series`. Landing stays on `AppFrame` (`OWNER_GRID_CLASS` still forces `[--grid-gutter:8px]`).
- `/k/[handle]` (and `/k/demo`) is the public kit — Pattern — shareable PitchKit Show code (`examples-pitchkit--shareable-pitch-kit`) plus Pattern — creator identity (public) (`examples-pitchkit--creator-identity-public`) for the nameplate: Avatar, display name (hide if missing), `@handle`, followers as context, optional Professional chip, Pitchkit-owned intro when non-empty (`examples-pitchkit--intro-public`; omit when empty — not Instagram biography), then followers / Engagement rate Stats, selected posts (likes/comments only), Contact `TextLink` when sourced, Past brands `{ id, name }` + letter Avatar when sourced (`examples-pitchkit--past-brands-public`; omit when empty). Unsigned visitors see Create your Pitchkit / Continue with Instagram (`showCreateBand`); hide for the kit owner and any signed-in Pitchkit user. No Edit / MoreMenu / hide / swap / Coming soon. Share link stays view even with an owner cookie. Owner PitchKit segment on `/insights` is Pattern — owner PitchKit (hide/restore + intro/brands editors), not this public URL. Hide empty contact and brands; no marquee; do not invent Verified, website, location, or brand year/summary. Account settings (`/settings` and owner Avatar) copies Pattern — account settings (owner) (`examples-pitchkit--account-settings-owner`): Dialog titled Account settings — Connected Instagram + Share kit `/k/[handle]` + Copy + Sign out + Disconnect + Delete account last (RE memo confirm). Dialog owns Delete; `/delete` is Meta review only.
- Six posts: last 30 days, **saves then reach then likes**. Engagement rate: `(likes + comments + saves + shares) / reach` (sum÷sum on posts with Insights `reach` > 0; unlocked 2026-09-18). Hide (or —) when reach is missing — do not fall back to ÷ followers. If Insights missing, hide Engagement rate, reach, and saves, and omit the optional chart (graph-unavailable). Spell **Engagement rate**.
- Hide from kit: `media.hidden_from_kit_at`. Public kit excludes hidden **before** `selectSixPosts`. Owner Insights includes all rows with the timestamp. ProofPosts partitions on init and when `posts` change: **"N shown"** + rank = `hidden_from_kit_at == null`; Hidden rows stay with Restore. `POST /api/media/hide` and `/api/media/restore` `{ mediaId }`, owner session only. Seed SoT is KV `HIDDEN_KIT` until Hyperdrive; when Hyperdrive is bound, SQL is SoT. httpOnly `pitchkit_hidden` is the owner reload mirror (public kit reads KV with no visitor cookie). No localStorage.
- Carousel: first frame. Video: poster only. R2 public read for kit images.
- Disconnect: `POST /auth/disconnect` stamps `disconnected_at` + nulls tokens (SQL when Hyperdrive is bound; else the KV Graph snapshot), clears session cookies, and 404s `/k/[handle]`. SQL + R2 `{user_id}/` purge still within 24 hours. `consent_index` default off. Sign out is cookie-only.
- Scopes: `instagram_business_basic` + `instagram_business_manage_insights` only.
- Postgres: [DATA.md](./DATA.md). Host is **Supabase Postgres via Hyperdrive**. Empty `detections` and `weekly_counts`. Stub and live OAuth use the same schema. SQL in `db/` (`npm run db:apply` once the direct URI exists). Client is postgres.js over `env.HYPERDRIVE.connectionString` when the binding is present — not `@supabase/supabase-js`. Hyperdrive origin is port 5432 (direct), not 6543 (pooler). Otherwise KV + `lib/seed.ts`. OAuth persist still falls back to KV `graph:` if a bound Hyperdrive SQL write fails or throws. Public `/k/demo` stays the seed; live OAuth finish never assigns session handle `demo` when `/me` username is not `demo`. Hyperdrive config `pitchkit` (`bf225442516d44f599e083b72df886cd`) is commented out in `wrangler.jsonc` (emergency KV-only; same id for preview MVP when re-enabled). Do not invent additional Hyperdrive ids. OpenNext typecheck: postgres.js rows go through `asQueryRows` (`unknown` → `T[]`) — do not `as T[]` (`Row & Iterable<Row>` does not overlap a caller `T`).
- `TOKEN_KEY` is a Workers secret. Never git. Encrypt tokens at rest when storing on the user row. Not required for the seed (tokens stay null). Optional operator `IG_USER_TOKEN` polls Graph for live users without an encrypted token — never for seed `demo`. Disconnect nulls those token fields on the snapshot so we stop polling as that user.
- **Graph:** host `graph.instagram.com`, pin `GRAPH_API_VERSION` (v25.0). Poll on Insights load when a token is present and `fetched_at`/`polled_at` is stale (&gt;6h) or Refresh. Map `CAROUSEL_ALBUM` → `CAROUSEL`, insights `saved` → `saves`. Skip Insights 400 (pre-Professional media) for medians; keep likes/comments. Insufficient reach series (missing / thin / all-zero with Insights) keeps the Reach Card empty band (muted **Badge** “No data” → title → body, title↔subtext `gap-2`) — do not hide the band or invent zeros. Graph-unavailable omits the optional chart. First-connect loading is the skeleton Pattern; Refresh retrieving is Header + `Chart.Loading`. Empty `follower_demographics` (0 rows / &lt;100) keeps the Audience Card insufficient well (muted **Badge** “No data” → `No audience data yet`, same `gap-2`) — never EXAMPLE % or `audience ?? SEED_AUDIENCE`. Never omit the Audience band.
- Cloudflare and Support (for now): **randy@whatmatters.so**. Supabase region: pick when we create the database.
- **UI:** `@whatmatters/wmds` (pattern-first, import `styles.css`). Layout Tailwind only (`grid-page` + `band` from CONSUMING). Cookie-gated Insights copies Pattern — creator Insights Show code (hug `SegmentedControl` in a three-column header; PitchKit segment is Pattern — owner PitchKit (Coming soon retired)). Public `/k/[handle]` copies Pattern — shareable PitchKit plus Pattern — creator identity (public), with `showCreateBand` for unsigned visitors. Account settings copies Pattern — account settings (owner). Landing `AppFrame` still uses Tailwind `[--grid-max:1140px] [--grid-column-gap:8px] [--grid-gutter:8px]`. Do not mount `GridOverlay` in product chrome. Do not invent a Pitchkit Grid atom. Do not compose AppShell. Lucide via WMDS. No shadcn. No Storybook in this repo. Pin `github:thewhatmatters/wmds#96f44587b3b3ff1c44de9f1e5adba61a661d30e1`. Intro + past brands copy `examples-pitchkit--intro-owner` / `--intro-public` / `--past-brands-owner` / `--past-brands-public`. Persist `intro` + `past_brands` on the KV Graph snapshot (Hyperdrive parked). After install, copy Geist into `node_modules/@whatmatters/wmds/dist/files`.
- **App:** Next.js App Router, TypeScript, Tailwind v4, official OpenNext on Workers. Workers Builds non-prod is `npx wrangler versions upload`; `wrangler.jsonc` `build.command` runs `npx opennextjs-cloudflare build` first. Charts: WMDS `Chart` only (`@visx/visx` peer). No Nivo in this `package.json`.
- **Verify:** project-local skill [`.cursor/skills/verify-pitchkit/`](./.cursor/skills/verify-pitchkit/SKILL.md). Feature map in `features/`. Prefer live https://pitchkit.app. Dry-run: `control-pitchkit doctor` → drive `insights-overview` → evidence under `artifacts/insights-overview/`.

## Do not build

CV, TikTok, PDF, brand dashboard, kit-view analytics for sale, Browser Run, D1, Vercel, shadcn, Storybook here, Instagram biography/website/rates/contact/geo on the kit. Pitchkit-owned intro + past brands (`{ id, name }`) are on the kit via KV Graph payload.

Do not add Graph columns we do not get from public posts + Insights. Do not invent rules that contradict [PLAN.md](./PLAN.md). Do not invent WMDS atoms here.

## After each turn

Locks → [PLAN.md](./PLAN.md). Picture → [ARCHITECTURE.md](./ARCHITECTURE.md). Columns → [DATA.md](./DATA.md). Stats words → [GLOSSARY.md](./GLOSSARY.md). Humans → [README.md](./README.md).
